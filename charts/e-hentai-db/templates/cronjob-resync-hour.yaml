apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: {{ include "e-hentai-db.fullname" . }}-resync-hour
  labels:
    {{- include "e-hentai-db.labels" . | nindent 4 }}
spec:
  schedule: "{{ .Values.cronjobs.resync_hour.schedule }}"
  jobTemplate:
    spec:
      template:
        spec:
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          restartPolicy: Never
          containers:
          - name: gallery-resync-hour
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
            imagePullPolicy: Always
            command:
            - npm
            - run
            - resync
            - "{{ .Values.cronjobs.resync_hour.hours }}"
            volumeMounts:
              - name: config-volume
                mountPath: /home/node/app/config
          volumes:
            - name: config-volume
              configMap:
                name: {{ include "e-hentai-db.fullname" . }}
                items:
                  - key: config.js
                    path: config.js
                  - key: .cookies.netscape
                    path: .cookies.netscape