apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "e-hentai-db.fullname" . }}-test-connection"
  labels:
    {{- include "e-hentai-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "e-hentai-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
